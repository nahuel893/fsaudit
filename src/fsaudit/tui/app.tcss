/* fsaudit TUI — base layout styles */

Screen {
    background: $surface;
    layout: vertical;
}

#lbl-health {
    text-align: center;
    padding: 1;
    width: 100%;
    border: solid $accent;
    height: auto;
}

#lbl-path {
    padding: 1;
    color: $text-muted;
}

#lbl-search-hint {
    padding: 0 1;
    color: $text-muted;
}

#lbl-search-status {
    padding: 0 1;
    height: auto;
}

.field-label {
    margin-top: 1;
    color: $text-muted;
}

.error {
    color: $error;
    padding: 0 1;
}

Button {
    margin: 1 1 0 0;
}

#action-bar {
    height: 3;
    align: center middle;
    padding: 0 1;
}

#action-bar Button {
    margin: 0 1;
}

#progress {
    margin: 1 0;
    height: auto;
}

#log {
    height: 1fr;
    max-height: 60%;
    border: solid $accent;
}

DataTable {
    height: 100%;
}

TabbedContent {
    height: 1fr;
}
